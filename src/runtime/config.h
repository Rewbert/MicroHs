#ifndef CONFIG_H
#define CONFIG_H

/*
 * In here are items that can be configured, but that does not depend on the platform.
 */

#define HEAP_CELLS 5000
#define STACK_SIZE 1000 // if I change to 2000 or 10000 it throws memerr out of memory

#endif // CONFIG_H
